# ===============================
# Dareloom.fun - Redirect Rules (SEO + SPA)
# Optimized for Cloudflare Pages
# ===============================

# 🔗 Redirect www → non-www (canonical)
https://www.dareloom.fun/* https://dareloom.fun/:splat 301

# 🧭 Redirect HTTP → HTTPS
http://dareloom.fun/* https://dareloom.fun/:splat 301

# 🗺️ Direct access to sitemap & robots (no redirect loops)
# Prevent redirect loops for XML and TXT
/sitemap.xml          /sitemap.xml 200
/sitemap-index.xml    /sitemap-index.xml 200
/sitemap-actors.xml   /sitemap-actors.xml 200
/sitemap-video.xml    /sitemap-video.xml 200
/sitemap-seo.xml      /sitemap-seo.xml 200
/robots.txt           /robots.txt 200
/indexnow-key.txt     /indexnow-key.txt 200

# 🎬 Redirect old /video/ URLs → /movies/
/video/* /movies/:splat 301

# 🔍 SEO-friendly short links
/latest     /movies/latest.html 301
/trending   /trending.html 301
/actresses  /actors.html 301

# 🌐 SPA fallback (must be last)
/* /index.html 200

# ===============================
# End of File
# ===============================
